# Релиз 247 — 09.09.2026

- Новый стартовый фон: сине-графитовая тонировка 80%, плавный чёрный градиент снизу.
- Входной купон: автогенерация кода, телефонная цифровая клавиатура, автоматическая точка, состояния замка и ручной свайп. Реальная авторизация остаётся серверной через Telegram; браузерный preview ограничен localhost.
- Компактные меню, карточки событий, сводки и источники; цвета разделов и контурная нижняя навигация.
- RU/EN для главного интерфейса и карточек, включая повторную отрисовку событий. Тренажёр пока отдельный русскоязычный прототип.
- Киберспорт API-Sport / Pari: просмотр исходов, тоталов, фор и ожидания линии. Купоны киберспорта пока не подключены.
- Live-тренажёр: авторские сценарии без запросов к поставщику и без реальных ставок; результаты хранятся отдельно в браузере.
- Движение коэффициентов: фон и тонкая стрелка внутри ячейки без сдвига цифр. The Odds ограничен одним букмекером onexbet.

## Проверка и выпуск

Локально проверены auth coupon, welcome, language, compact header, esports UI,
live trainer, trainer return, odds delta/full UI. Python: аккаунты, приватный API,
киберспорт и retry; зависимости проверены pip check. Тесты используют фикстуры.

При выкладке: резервная копия серверных SQLite, fast-forward на main,
секрет API-Sport отдельно по SSH с правами 600, сохранённый кэш отдельно от Git.
Серверные аккаунты, купоны и конфигурация не заменяются локальными базами.
Ссылка меню бота обновлена до v=247. Ручная проверка в Telegram — после выпуска.

## Результат развёртывания

Код: ebc2134, GitHub main и VPS /opt/freebk/app синхронизированы.
20 изменённых файлов интерфейса по HTTPS побайтно совпали с Git после
нормализации переводов строк. Мобильная проверка опубликованной страницы:
купон, автогенерация, клавиатура, отсутствие горизонтального переполнения — PASS.
Запросы к поставщикам во время этой проверки блокировались.
API health и esports — HTTP 200; 146 событий из кэша без ошибки.
Telegram getChatMenuButton подтвердил https://159-194-244-46.sslip.io/mini-app.html?v=247.
freebk-bot active, NRestarts=0; обе SQLite прошли integrity_check.
Резервная копия: /opt/freebk/backups/state-20260909T204541139544Z.
Временные транспортные копии секрета и кэша удалены после сверки; рабочие
копии сохранены. Вложенный независимый репозиторий «мой телега бот» не изменён.
Проверка настоящего Telegram ID и жестов на телефоне остаётся владельцу.
