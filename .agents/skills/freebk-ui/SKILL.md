---
name: freebk-ui
description: Maintain the FreeBK Telegram Mini App layout, mobile gestures, fight grouping and visual conventions when editing this repository's interface or feed rendering.
---

# FreeBK: мобильный интерфейс и совместная работа

Текущий видимый бренд: P2P Market (новое название от 07.09.2026).
FreeBK/freebk в технических идентификаторах и хранилище сохранять для совместимости.

Договорённости на 7 сентября 2026. Это проектный скилл: переносить вместе
с репозиторием, не ограничиваться памятью чата. Последующее явное решение
пользователя имеет приоритет; обновлять этот файл при утверждении изменений.

## Рабочий процесс

- Общаться по-русски, коротко объяснять видимое изменение. Различать
  сделанное, проверенное в браузере и ещё не проверенное на телефоне.
- Локальные доработки не означают разрешения на commit/push/deploy.
- Перед сменой цвета читать `source/DESIGN_COLORS.md` от корня репозитория.
  Цвет отмены, незахода и иконок — разные понятия. «Заход» пока не утверждён.
- Не переоформлять соседние разделы без необходимости. Сохранять текущие
  пользовательские изменения и содержимое API; неизвестные сведения не выдумывать.

## Главный список

- Последнее уточнение 11.09: «Трансляции» и «Live тренажёр» находятся
  в боковом меню профиля, не в верхней навигации. Их существующие экраны сохранены.

- Киберспорт (11.09): карточки используют компактную геометрию единоборств:
  заголовок от 44 px, строки матча от 48 px, команды/котировки 56,7%/43,3%,
  имена 12 px/600, цена 17 px/750, время 9 px/500. Счёт и карты сохраняются;
  длинные названия переносятся и увеличивают высоту без обрезания.

- Входной купон (11.09.2026): две строки — автогенерация коэффициента
  за 2000 ms с замочком справа и ввод кода ниже. Средняя отдельная
  строка кода и отпечаток удалены. Замок закрыт до правильного ввода,
  затем открывается. Уточнение: сначала компактный купон снизу из двух
  строк и бегунка, клавиатура скрыта. Купон увеличен примерно на 20% и
  остаётся внизу неподвижно. После генерации прозрачная клавиатура с
  чёрными круглыми кнопками появляется справа над купоном, не перекрывая
  логотип. Размер клавиш подстраивается под свободный промежуток.
  Круглый крестик закрытия в углу входного купона скрыт; закрытие снаружи,
  ручкой и Escape сохраняется. В остальных меню кнопки закрытия не менять.
  Блик USB-логотипа проходит по чёрной поверхности кружков под цифрами,
  а сами цифры остаются белыми, без блика. Тот же цвет, маска,
  тайминг и общая фаза при повторном открытии; reduced-motion отключает блик.

- Верхние вкладки разделов: активность показывать цветом, без подчёркивания
  Уточнение 11.09: «Трансляции» вынесены в верхнюю строку после Live / Прематч.
  Карусель: Единоборства, Бокс, Киберспорт, P2P Betting, Poker, Trading,
  Live тренажёр. Все ячейки по ширине подписи с общим интервалом; подпись
  Live тренажёр уменьшена до одной строки. Это заменяет прежние размеры ячеек.
  подписей (09.09.2026). Фокус клавиатуры сохранять.
  Уточнение 11.09: иконки и подписи на общей линии; у «Трансляции» точка
  6 px и счётчик располагаются справа от иконки экрана, не под подписью.

- Уточнение владельца 09.09.2026: максимально освобождать экран событий.
  Новое решение 11.09.2026 заменяет запрет горизонтальной прокрутки для
  верхней ленты: после пяти текущих разделов добавлены P2P Betting, Poker,
  Trading с пустыми экранами. Вся лента прокручивается пальцем; размеры
  первых пяти сохранены на узком экране; на широком расстояния уплотнены
  (базовая ширина до 64 px). При загрузке и входе лента начинается с первых
  пяти разделов. Рынки и остальная страница не прокручиваются по X.
  Навигация и рынки компактные, все вкладки помещаются без горизонтальной
  прокрутки и обрезания. Не маскировать переполнение скрытием scrollbar.
  Подробная сводка источника («MMA: … событий», время обновления) свёрнута
  по умолчанию; раскрывается только пользователем. Вертикальная прокрутка
  длинного списка событий остаётся. Проверять 320/390 px; при расширении
  навигации адаптировать сетку/перенос, не возвращать горизонтальную ленту.

- Стартовый экран гостя (08.09.2026): постоянное белое полупрозрачное
  стекло с размытым настоящим интерфейсом позади; чёрный курсивный жирный
  «P2P Market Free Bet» и контурный подарок. Надпись плавно проявляется
  от opacity 0 до 1 за 1100 ms, без смещения; учитывать reduced-motion.
  После проявления по контурам букв и подарка проходит слева направо
  тонкий салатовый блик (#A3C85A / #E6F0CD), цикл 3 секунды с паузой.
  Чёрная заливка сохраняется; блик отключён при reduced-motion.
  Не убирать по таймеру или
  окончанию загрузки. «Войти» открывает компактную нижнюю панель текущей
  авторизации. После подтверждённого входа открывается приложение; при
  выходе заставка возвращается. Регистрация пока только по приглашению,
  не имитировать публичную регистрацию. Файлы: docs/welcome.css и .js.

- Mobile-first: ширина приложения ограничена 450 px, проверять 390 и 320 px.
  Не создавать отдельный десктопный дизайн. Текст переносится, не обрезается.
- Главная плашка: сначала дата и время МСК, потом название/пара,
  описание (главный бой, вес, раунды) продолжает название в той же строке
  через разделитель; перенос только по доступной ширине, без обрезания.
  затем вес, раунды, статус. Ссылки на источники (09.09.2026) только
  внутри раскрытых боёв возле времени/линии; у бокса — внутри карточки боя.
- Типографика главных плашек: название 12 px, расписание 9 px / 500 (09.09.2026),
  оригинальная подпись 10 px, описание 11 px. Отступы 8×12 px,
  минимальная высота области нажатия 44 px.
- Расписание MMA/бокса и их активные вкладки (09.09.2026): однотонный
  светло-синий #397EAE без градиента. Расписание облегчённое, 500;
  конкретные значения находятся в палитре. Внутри турниров — только
  часы и минуты МСК, полная дата остаётся в datetime/title.
- MMA: подтверждённые турниры группируются; бои с отдельным highlight
  (главный, титульный, рейтинговый) выделяются по id в самостоятельные
  раскрывающиеся плашки и исключаются из общего блока. Не копировать
  главную пару турнира в заголовок другого выделенного боя.
- Подтверждённый MMA: пара бойцов один раз по-русски; описание ниже
  не повторяет эту пару. Не добавлять английский дубль названия.
  В «В разработке / Возможные» английские имена пока сохранены.
- Бокс: один бой — одна плашка, без префикса «Бокс» и промежуточного
  раскрытия. Нажатие сразу вызывает существующую большую карточку
  `openFight(id)`. Источники кликаются независимо. Будущую роспись
  проектировать отдельно, пока её не придумывать.
- Единый шеврон `›`: одинаковый размер и цвет в обоих разделах;
  в MMA поворот на 90° вниз при раскрытии, 200 ms, reduced-motion учтён.
- Внутри MMA имена 12 px, оригиналы 10 px, время 10 px; компактные
  Уточнение 09.09.2026: имена 12 px / 600, без английского дубля под ними;
  фаворит/андердог остаются тонкими. RU/EN переводит представление, не данные.
  отступы. Сохранять сведения о карде, весе, раундах и источниках.
- Внутри каждого турнира «Ранние прелимы», «Прелимы», «Основной кард»
  показывать только у первого боя соответствующего последовательного блока.
  Отдельные отметки главного/соглавного боя, титула и раундов сохранять.
  Скрывать повтор только в представлении, не изменять данные API.

## Жесты и обновление

- Уточнение 11.09: меню гамбургера — боковая панель слева на полную
  высоту, до 340 px шириной, профиль сверху, баланс ниже, затем пункты,
  выход внизу. Закрытие крестиком, снаружи и Escape. Входной купон не менять.
- Входной купон: нижняя панель; открытие 420 ms с быстрым началом
  и замедлением cubic-bezier(.16,1,.3,1). Ручка имеет область 90×44 px.
- Панель следует за пальцем за ручку, закрывается длинным/быстрым
  свайпом вниз, короткий жест возвращает её. pointercancel возвращает
  панель; прокрутка содержимого независима. Сохранять Escape, кнопку
  закрытия, reduced-motion и возможность повторного открытия.
- Трансляция — отдельный экран `broadcasts.html`: компактный ТВ-плеер
  180×180 с разворачиванием на экран и вкладка «Стрим» с тремя местами.
  Купон и кнопка «Смотреть» удалены по решению владельца. Видеосвязь
  подключается по нажатию через внешний Jitsi (комната по случайной ссылке).
  Отдельный предпросмотр своей камеры не пишет и не отправляет видео,
  не запрашивает микрофон. При выходе камера и звонок отключаются.
  При переключении на «Стрим» ТВ-плеер останавливается.
- Обновление конкретного боя не равно общему обновлению. Общее
  обновляет счётчики и события согласованно. Сохранять индикацию загрузки
  и различие успешного ответа без изменения котировок и ошибки поставщика.

## Стабильность при новом ответе API

Правила оформления живут в коде, не в ответе модели или ручной правке DOM.
API поставляет данные, но не CSS/HTML. Сохранять нормализацию, экранирование,
устойчивые id, сортировку по времени и состояние раскрытия при render.
Количество карточек, высота при длинном тексте и порядок событий могут
естественно изменяться с данными; не обещать неизменность каждого пикселя.

Ключевые файлы: `docs/tournament-groups.js`, `docs/ufc-screen.js`,
`docs/feed-runtime.js`, `docs/freebk.js`, `docs/odds-updates.js`,
`docs/mini-app.html`, `docs/broadcasts.*`.

После изменений проверять браузером оба раздела, раскрытие/закрытие,
длинные подписи, отсутствие горизонтального скролла и повторный render
через `FightScreen.setEvents` с теми же и изменёнными данными. Не расходовать
квоту поставщика ради визуальной проверки: использовать кэш или фикстуры.
Запускать релевантные `test_*.cjs`; тесты не заменяют визуальную проверку.
Удостовериться, что главная дата остаётся, а внутри показано только время.
